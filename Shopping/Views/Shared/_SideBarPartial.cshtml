<div class="left-sidebar shadow-sm p-4 mb-4 bg-white rounded-3">
    <!-- Categories Dropdown -->
    <div class="dropdown mb-4">
        <h4 class="fw-semibold mb-3 text-dark">Danh mục sản phẩm</h4>
        <button class="btn btn-outline-primary w-100 dropdown-toggle shadow-sm" type="button"
                data-bs-toggle="dropdown" aria-expanded="false">
            Chọn danh mục
        </button>
        <ul class="dropdown-menu w-100 border-light shadow-sm">
            <vc:categories></vc:categories>
        </ul>
    </div>

    <!-- Brands Dropdown -->
    <div class="dropdown">
        <h4 class="fw-semibold mb-3 text-dark">Thương hiệu</h4>
        <button class="btn btn-outline-primary w-100 dropdown-toggle shadow-sm" type="button"
                data-bs-toggle="dropdown" aria-expanded="false">
            Chọn thương hiệu
        </button>
        <ul class="dropdown-menu w-100 border-light shadow-sm">
            <vc:brands></vc:brands>
        </ul>
    </div>
</div>

<style>
    .left-sidebar {
        min-width: 250px;
        background: linear-gradient(145deg, #ffffff, #f8f9fa);
        border: none;
        transition: all 0.3s ease;
    }

        .left-sidebar:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.1) !important;
        }

    .btn-outline-primary {
        border-color: #007bff;
        color: #007bff;
        padding: 10px 15px;
        border-radius: 8px;
        transition: all 0.3s ease;
    }

        .btn-outline-primary:hover {
            background-color: #007bff;
            color: white;
            border-color: #007bff;
            box-shadow: 0 4px 15px rgba(0,123,255,0.3);
        }

    .dropdown-menu {
        max-height: 300px;
        overflow-y: auto;
        border-radius: 8px;
        padding: 10px 0;
        border: 1px solid #e9ecef;
    }

    .dropdown-item {
        color: #495057;
        padding: 8px 20px;
        border-radius: 4px;
        margin: 2px 10px;
        transition: all 0.2s ease;
    }

        .dropdown-item:hover {
            color: #007bff;
            background-color: #f1f3f5;
            transform: translateX(5px);
        }

    h4 {
        font-size: 1.25rem;
        letter-spacing: 0.5px;
        background: linear-gradient(to right, #facc15, #fbbf24);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
</style>